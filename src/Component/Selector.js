import React from 'react'
// import Button from './Button'

const Selector = () => {
    for( var i = 0; i < 10; i++){
        <button>{i}</button>
    }
    
  return (
    <div>
        <h1>React Selector</h1>
    </div>
  )
}

export default Selector